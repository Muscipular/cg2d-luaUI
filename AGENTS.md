# AGENTS.md

## 全局规则

1. 只允许使用中文回答，输出文件默认为中文。
2. 默认不进行编译或者调试。
3. 保持代码整洁，每次只进行最小化调整。
4. 禁止直接引入外部依赖，但可以提示用户。
5. 项目所有文件使用 GBK 编码。
6. 读写文件必须显式指定 GBK，禁止使用 Default 编码。
7. 禁止过度联想：仅实现用户要求；不扩展未要求的功能；不补充未说明的业务逻辑；不重构用户代码。
8. 输出必须简洁精准，仅提供最小化可用代码或说明。
9. 任何不确定的地方要求用户澄清。

## 项目规则

1. `init.lua` 是唯一入口，只负责加载架构文件和 `luaUI\ModuleConfig.lua`。
2. 业务模块放入 `luaUI\modules\` 目录。
3. `luaUI\ModuleConfig.lua` 控制默认加载模块。
4. `luaUI\doc\` 目录用于文档和接口声明，不作为运行入口。

## 模块规则

1. 模块文件必须返回模块类表。
2. 模块类通过 `ModuleBase:extend(name)` 创建。
3. 初始化逻辑写入 `onLoad`。
4. 自定义清理逻辑写入 `onUnload`。
5. 事件句柄优先使用 `ModuleBase` 代理注册，便于卸载时自动释放。
6. 窗口优先使用 `self:newWindow` 创建，便于卸载时自动关闭。

## 文档规则

1. 架构说明写入 `luaUI\doc\README.md`。
2. 项目说明写入根目录 `README.md`。
3. 接口声明优先更新 `luaUI\doc\` 目录下对应文档。